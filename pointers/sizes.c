#include <stdio.h>

int main(int argc, char *argv[]){
  printf("size of short is %d\n", sizeof(short));
  printf("size of int is %d\n", sizeof(int));
  printf("size of long is %d\n", sizeof(long));

  return 0;
}
